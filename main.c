#include "main.h"

int main(void)
{
	int a = 0, b = 0;

    a = _printf("%s", "hi every one");
    b = printf("%s", "hi every one");

    printf("test    - %d\nanswer  - %d\n", a, b);

    return (0);
}
